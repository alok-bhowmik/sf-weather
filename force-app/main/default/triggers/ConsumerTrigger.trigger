trigger ConsumerTrigger on Account (after insert) 
{
    //if(trigger.isInsert && trigger.isAfter)
    //{
       // for(Account acc : trigger.new)
        //{
         //ConsumerAccountMonthly con = new ConsumerAccountMonthly();
         //con.start(null,[Select Id, Name,RecordType.Name From Account Where ID IN :trigger.new]);
           
           //mybatchclass b = New mybatchclass()
         //database.executeBatch(con);
        //}

   // }
}